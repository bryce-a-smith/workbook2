"use strict";

var x1 = -2;
var y1 = 1;

var x2 = 3;
var y2 = 4;

var distance = Math.sqrt((x2 - x1) * 2 + (y2 - y1) * 2);

console.log("The distance between (" + x1 + "," + y1 + ") and (" + x2 + "," + y2 + ") is " + distance.toFixed(1));
